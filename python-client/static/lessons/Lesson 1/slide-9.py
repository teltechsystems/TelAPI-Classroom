# This is how we import a library
import random

# Let's generate a random number!
random_number = random.randint(0, 100)

# What do you think the random number will be?
print "The random number is: %s" % random_number